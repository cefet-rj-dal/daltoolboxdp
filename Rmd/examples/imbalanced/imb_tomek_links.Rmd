# Imbalanced Resampling: Tomek Links

```{r}
library(daltoolbox)
library(daltoolboxdp)

model <- create_imb_tomek_model()
res   <- fit_imb_resample_tomek(model, df, "Class")

X_res <- res[[1]]
y_res <- res[[2]]
```
