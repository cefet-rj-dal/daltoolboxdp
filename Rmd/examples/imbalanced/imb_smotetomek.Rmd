# Imbalanced Resampling: SMOTE + Tomek Links

```{r}
library(daltoolbox)
library(daltoolboxdp)

model <- create_imb_smotetomek_model(random_state = 42)
res   <- fit_imb_resample_smotetomek(model, df, "Class")

X_res <- res[[1]]
y_res <- res[[2]]
```
