# Feature Selection: SelectKBest

```{r}
library(daltoolbox)
library(daltoolboxdp)

model <- create_fe_selectkbest_model(k = 5)
X_sel <- fit_transform_fe_selectkbest(model, df, "Class")

dim(X_sel)
```
